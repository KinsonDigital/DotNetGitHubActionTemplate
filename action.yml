name: '.NET Github Action Sample' # TODO: Set the name
description: 'An example NET Github action.' # TODO: Set the description
branding:
  icon: sliders
  color: purple
inputs:
  message:
    description: 'The message to print to the console.'
    required: true
# TODO: Add as many inputs as needed

# TODO: Re-enable if you want to have action outputs
#outputs:
#  sample-output: # This is the name of the output
#    description: 'Sample output.'

# These are the arguments to pass into the console app
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
  - '-m'
  - ${{ inputs.message }}
